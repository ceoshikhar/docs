---
title: "Check recipient's preference"
openapi: "GET /v1/recipients/{recipient_id}/preferences/targets"
---

💡 You could use this API to conditionally render something based on whether the recipient has subscribed to the target or not.

`inherited: false` means the recipient **has** overridden the preference for that `target` explicitly to whatever `subscribed` is right now.
