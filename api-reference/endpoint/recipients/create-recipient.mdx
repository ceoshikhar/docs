---
title: "Create recipient"
openapi: "POST /recipients"
---

If a recipient with the given `id` already exists, this will return a conflict error.

💡 You should use this API during your user sign up flow.
